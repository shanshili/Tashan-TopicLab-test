# Docker Compose 配置（本地开发）
# 复制此文件为 .env 并按需修改
# cp .env.example .env
# 线上部署请使用 .env.deploy.example → GitHub Secrets DEPLOY_ENV

# === Docker 端口与挂载 ===
WORKSPACE_PATH=./workspace
LIBS_PATH=./backend/libs
FRONTEND_PORT=3000
BACKEND_PORT=8000

# === 后端 API（必填，否则后端启动失败）===
# 参考 backend/.env.example 获取完整说明
ANTHROPIC_API_KEY=test
ANTHROPIC_BASE_URL=
ANTHROPIC_MODEL=
AI_GENERATION_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
AI_GENERATION_API_KEY=test
AI_GENERATION_MODEL=test
