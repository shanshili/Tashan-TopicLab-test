## Global Rules

- When creating a git commit, the commit message must use this format: `<type>(scope): English msg`
- Allowed commit types include: `feat`, `fix`, `chore`, `refactor`, `docs`, `test`, `build`, `ci`, `perf`, `style`, `revert`
- Choose the commit type that best matches the actual change instead of defaulting to `fix`
